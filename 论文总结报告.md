# 论文总结报告

**论文标题：** A high-order implicit time integration method for linear and nonlinear dynamics with efficient computation of accelerations

**作者：** Daniel O'Shea, Xiaoran Zhang, Shayan Mohammadian, Chongmin Song

---

## 第一部分：摘要解读

本文提出了一类**自启动、高阶、隐式时间积分格式**的全新算法，能以用户指定参数 $\rho_{\mathrm{ss}} \in [0,1]$ 精确调控数值耗散，适用于线性与非线性瞬态动力问题。针对已有同类方法（Padé展开方案与复合 $M$ 格式）的核心短板——在每个时步内必须计算 $\mathbf{M}^{-1}\mathbf{f}$（质量矩阵分解）以及需要额外方程求解才能获得加速度——本文通过对有理近似分子/分母多项式作**部分分式展开**，彻底消除了对质量矩阵分解的依赖：外力项只需在时步内的若干**Gauss-Lobatto 采样点**处对力向量 $\mathbf{f}$ 直接求值，无需任何 $\mathbf{M}^{-1}$ 运算。此外，文章推导了一种仅用**向量运算**即可获得与位移/速度同阶精度加速度的新公式，无需额外求解任何方程组。在数值精度方面，所提算法：（1）外力与物理阻尼的存在不降低精度阶次；（2）等效刚度矩阵保持与梯形法则相同的稀疏性与规模，大规模问题计算效率高；（3）对非线性问题同样实现了与线性问题相同的收敛阶次。针对波传播问题，与商业软件中常用的 HHT-$\alpha$ 法和显式中心差分法相比，高阶格式在抑制虚假高频振荡、给出可靠加速度响应方面具有显著优势。

---

## 第二部分：问题的核心与解决思路

### 2.1 本文要解决的核心问题

**旧算法的核心瓶颈体现在以下时步推进方程中：**

$$
\mathbf{z}_n = \frac{\mathbf{P}}{\mathbf{Q}} \mathbf{z}_{n-1} + \frac{1}{\mathbf{Q}} \sum_{k=0}^{p_f} \mathbf{C}_k \begin{Bmatrix} \Delta t^2 \mathbf{M}^{-1} \tilde{\mathbf{f}}^{(k)} \\ \mathbf{0} \end{Bmatrix}
$$

其中 $\mathbf{P} = P(\mathbf{A})$，$\mathbf{Q} = Q(\mathbf{A})$ 是系数矩阵 $\mathbf{A}$ 的 $M$ 阶多项式，$\mathbf{C}_k$ 是递推得到的多项式矩阵，状态向量 $\mathbf{z} = [\dot{\mathbf{u}},\ \mathbf{u}]^T$。

上式的非齐次项中存在 $\mathbf{M}^{-1} \tilde{\mathbf{f}}^{(k)}$，意味着：

- **问题一：必须对质量矩阵 $\mathbf{M}$ 进行 LU 分解**，这对使用一致质量矩阵的大规模问题代价高昂。
- **问题二：对非线性问题**，$\mathbf{f}$ 依赖于当前时步的未知状态 $\mathbf{z}(t)$，须在迭代过程中反复计算 $\mathbf{M}^{-1}\mathbf{f}$，增加额外计算量。

**加速度问题：** 旧算法无法在不额外求解方程组的前提下获得同阶精度的加速度。若要保持与位移/速度相同的精度阶次，就不得不从运动方程直接求解加速度，这又一次引入 $\mathbf{M}^{-1}$，抵消了自启动算法本应带来的计算优势。

---

### 2.2 文章的解决方法与核心公式推导

#### （1）状态空间方程与矩阵指数精确解

设结构动力问题的运动方程为：

$$
\mathbf{M}\ddot{\mathbf{u}} + \mathbf{C}_{n-1}\dot{\mathbf{u}} + \mathbf{K}_{n-1}\mathbf{u} = \mathbf{f}(\mathbf{u}, \dot{\mathbf{u}})
$$

引入无量纲时间 $s \in [0,1]$（其中 $t = t_{n-1} + s\Delta t$），并构造状态向量 $\mathbf{z} = [\overset{\circ}{\mathbf{u}},\ \mathbf{u}]^T$（圈上标表示对 $s$ 的导数），方程化为一阶 ODE 系统：

$$
\frac{d\mathbf{z}}{ds} = \mathbf{A}\mathbf{z}(s) + \bar{\mathbf{f}}(\mathbf{z}(s))
$$

其中切线系数矩阵为：

$$
\mathbf{A} = \begin{bmatrix} -\Delta t \mathbf{M}^{-1}\mathbf{C}_{n-1} & -\Delta t^2 \mathbf{M}^{-1}\mathbf{K}_{n-1} \\ \mathbf{I} & \mathbf{0} \end{bmatrix}
$$

精确解由矩阵指数给出：

$$
\mathbf{z}(t_{n-1} + s\Delta t) = e^{\mathbf{A}s}\mathbf{z}_{n-1} + e^{\mathbf{A}s}\int_0^s e^{-\mathbf{A}\tau}\bar{\mathbf{f}}\bigl(\mathbf{z}(t_{n-1}+\tau\Delta t)\bigr)\,d\tau
$$

#### （2）用有理近似代替矩阵指数

用 Padé 展开或单重实根有理函数近似 $e^{\mathbf{A}}$：

$$
e^{\mathbf{A}} \approx R(\mathbf{A}) = \frac{P(\mathbf{A})}{Q(\mathbf{A})} = \frac{p_0\mathbf{I} + p_1\mathbf{A} + \cdots + p_M\mathbf{A}^M}{q_0\mathbf{I} + q_1\mathbf{A} + \cdots + q_M\mathbf{A}^M}
$$

高频极限处的谱半径为 $\rho_\infty = |p_M/q_M|$，由用户指定控制数值耗散。

非齐次项中的外力通过在时步中点做 $p_t$ 阶 Taylor 展开处理：

$$
\mathbf{f}(s) = \sum_{k=0}^{p_f} \tilde{\mathbf{f}}^{(k)}(s-0.5)^k
$$

$\mathbf{C}_k$ 矩阵多项式由递推公式确定（$k=0$ 起步，逐级递推）：

$$
\mathbf{C}_0 = \mathbf{A}^{-1}(\mathbf{P} - \mathbf{Q})
$$

$$
\mathbf{C}_k = \mathbf{A}^{-1}\!\left(k\mathbf{C}_{k-1} + \left(-\tfrac{1}{2}\right)^k\!\left(\mathbf{P} - (-1)^k\mathbf{Q}\right)\right), \quad k = 1,2,\ldots,p_f
$$

由于 $\mathbf{P}$、$\mathbf{Q}$ 均为 $M$ 阶多项式，$\mathbf{C}_k$ 最多为 $M-1$ 阶多项式，其系数矩阵记为 $\mathbf{c} = [c_{ki}]$：

$$
C_k(\mathbf{A}) = \sum_{i=0}^{M-1} c_{ki}\mathbf{A}^i
$$

#### （3）部分分式展开——消去 $\mathbf{M}^{-1}\mathbf{f}$ 的关键步骤

**情形一：分母 $Q$ 有 $M$ 个互异根 $r_i$（Padé 方案）**

将 $\mathbf{P}/\mathbf{Q}$ 分解为：

$$
\frac{\mathbf{P}}{\mathbf{Q}} = \frac{p_M}{q_M}\mathbf{I} + \sum_{i=1}^{M} \frac{a_i\, P_L(r_i)}{r_i\mathbf{I} - \mathbf{A}}
$$

其中部分分式留数为：

$$
a_i = \frac{1}{\prod_{j \neq i}(r_j - r_i)}
$$

对非齐次项同理展开 $C_k/\mathbf{Q}$，并**先对 $k$ 求和再做矩阵运算**，定义每个根 $r_i$ 对应的合并力向量：

$$
\mathbf{f}_i = \sum_{k=0}^{p_f} \tilde{\mathbf{f}}^{(k)} C_k(r_i) = \tilde{\mathbf{F}} \cdot \mathbf{c}\,\mathbf{r}_i, \quad \mathbf{r}_i = [1,\ r_i,\ r_i^2,\ \ldots,\ r_i^{M-1}]^T
$$

**$\mathbf{f}_i$ 中不含 $\mathbf{M}^{-1}$！** 时步方程最终变为：

$$
\mathbf{z}_n = \rho\,\mathbf{z}_{n-1} + \sum_{i=1}^{M} a_i\,\mathbf{x}_i
$$

每个辅助向量 $\mathbf{x}_i$ 由以下方程确定：

$$
(r_i\mathbf{I} - \mathbf{A})\cdot\mathbf{x}_i = \mathbf{g}_i + \begin{Bmatrix} \Delta t^2\mathbf{M}^{-1}\mathbf{f}_i \\ \mathbf{0} \end{Bmatrix}
$$

将 $\mathbf{A}$ 展开后，上半分量（速度部分）满足：

$$
\underbrace{\left(r_i^2\mathbf{M} + r_i\Delta t\mathbf{C} + \Delta t^2\mathbf{K}\right)}_{\text{等效刚度矩阵（与梯形法则同型）}}\hat{\mathbf{x}}_i = r_i\mathbf{M}\hat{\mathbf{g}}_i - \Delta t^2\mathbf{K}\bar{\mathbf{g}}_i + r_i\Delta t^2\mathbf{f}_i
$$

右端项仅需 $\mathbf{f}_i$（不含 $\mathbf{M}^{-1}$），从而**彻底消除质量矩阵分解的需求**。

**情形二：分母 $Q = (r\mathbf{I}-\mathbf{A})^M$ 只有一个 $M$ 重实根（M 格式）**

引入移位矩阵 $\mathbf{A}_r = r\mathbf{I} - \mathbf{A}$，将多项式系数从 $\mathbf{A}$ 基底移位至 $\mathbf{A}_r$ 基底，时步方程变为：

$$
\mathbf{z}_n = p_{rM}\mathbf{z}_{n-1} + \sum_{i=0}^{M-1}\frac{1}{\mathbf{A}_r^M}\mathbf{b}_i, \quad \mathbf{b}_i = p_{ri}\mathbf{z}_{n-1} + \begin{Bmatrix} \Delta t^2\mathbf{M}^{-1}\mathbf{f}_{ri} \\ \mathbf{0} \end{Bmatrix}
$$

利用 Horner 递推（嵌套运算），每一步求解：

$$
\mathbf{A}_r\cdot\mathbf{x}_i = \mathbf{b}_{i-1} + \mathbf{x}_{i-1}, \quad \mathbf{x}_0 = \mathbf{0},\quad i = 1,2,\ldots,M
$$

所有 $M$ 步共用**同一实等效刚度矩阵**（$r$ 相同），只需一次 LU 分解。最终：

$$
\mathbf{z}_n = p_{rM}\mathbf{z}_{n-1} + \mathbf{x}_M, \quad |p_{rM}| = \rho_\infty
$$

#### （4）仅用向量运算获得同阶精度加速度

对情形一（互异根），对时步方程关于 $s$ 求无量纲时间导数，速度分量满足：

$$
\overset{\circ}{\mathbf{u}}_n = \rho\,\overset{\circ}{\mathbf{u}}_{n-1} + \sum_{i=1}^{M} a_i\,\overset{\circ}{\hat{\mathbf{x}}}_i
$$

对辅助方程 $r_i\bar{\mathbf{x}}_i = \hat{\mathbf{x}}_i + \bar{\mathbf{g}}_i$ 关于 $s$ 求导，得：

$$
\overset{\circ}{\hat{\mathbf{x}}}_i = r_i\hat{\mathbf{x}}_i - \overset{\circ}{\bar{\mathbf{g}}}_i
$$

右端量 $\hat{\mathbf{x}}_i$ 和 $\overset{\circ}{\bar{\mathbf{g}}}_i$ 均在时步推进中已计算，无需任何额外方程求解，加速度为：

$$
\overset{\circ}{\mathbf{u}}_n = \rho\,\overset{\circ}{\mathbf{u}}_{n-1} + \sum_{i=1}^{M} a_i\!\left(r_i\hat{\mathbf{x}}_i - \overset{\circ}{\bar{\mathbf{g}}}_i\right)
$$

对情形二（单重实根），加速度类似地只需时步结束时的最后一个 $\mathbf{x}_M$：

$$
\overset{\circ}{\mathbf{u}}_n = p_{rM}\overset{\circ}{\mathbf{u}}_{n-1} + \left(r\hat{\mathbf{x}}_M - \overset{\circ}{\bar{\mathbf{g}}}_M\right)
$$

---

## 第三部分：论文整体流程

### 步骤 1：构建状态空间一阶 ODE 系统

将原始结构动力方程（含阻尼、刚度、非线性内力）写为：

$$
\mathbf{M}\ddot{\mathbf{u}} + \mathbf{C}_{n-1}\dot{\mathbf{u}} + \mathbf{K}_{n-1}\mathbf{u} = \mathbf{f}(\mathbf{u},\dot{\mathbf{u}})
$$

引入无量纲时间 $s = (t - t_{n-1})/\Delta t \in [0,1]$ 及状态向量 $\mathbf{z} = [\overset{\circ}{\mathbf{u}},\ \mathbf{u}]^T$，转化为一阶系统：

$$
\frac{d\mathbf{z}}{ds} = \mathbf{A}\mathbf{z} + \bar{\mathbf{f}}(\mathbf{z})
$$

**目的：** 利用矩阵指数 $e^{\mathbf{A}s}$ 写出精确解，为后续有理近似奠定基础。

---

### 步骤 2：矩阵指数的有理近似（Padé 展开 或 单重实根选取）

**Padé 方案**（互异根，精度 $2M$ 或 $2M-1$）：

$$
e^{\mathbf{A}} \approx \frac{P(\mathbf{A})}{Q(\mathbf{A})} = \frac{\rho_\infty P_{M/M}(\mathbf{A}) + (1-\rho_\infty)P_{(M-1)/M}(\mathbf{A})}{\rho_\infty Q_{M/M}(\mathbf{A}) + (1-\rho_\infty)Q_{(M-1)/M}(\mathbf{A})}
$$

其中混合阶 Padé 系数为：

$$
P_{L/M} = \sum_{i=0}^{L}\frac{(M+L-i)!}{i!(L-i)!}\mathbf{A}^i, \quad Q_{L/M} = \frac{M!}{L!}\sum_{i=0}^{M}\frac{(M+L-i)!}{i!(M-i)!}(-\mathbf{A})^i
$$

参数 $\rho_\infty = 1$ 时 $A$ 稳定无耗散，$\rho_\infty = 0$ 时 $L$ 稳定最大耗散。

**M 格式**（单重实根，精度 $M$）：

$$
e^{\mathbf{A}} \approx \frac{P(\mathbf{A})}{(r\mathbf{I} - \mathbf{A})^M}
$$

根 $r$ 由以下稳定性多项式方程求解（以 $M=3$ 为例）：

$$
-1 + 3r - \frac{3}{2}r^2 + \frac{1}{6}r^3 = -\rho_\infty
$$

选取使谱半径 $|\rho| \le 1$ 且低频相对周期误差最小的根（$M=3$，$\rho_\infty=0.125$ 时 $r=2.3917$）。

---

### 步骤 3：力向量在时步内的多项式展开与采样

外力在时步中点（$s=0.5$）处展开为 $p_f$ 阶 Taylor 多项式：

$$
\mathbf{f}(s) = \sum_{k=0}^{p_f}\tilde{\mathbf{f}}^{(k)}(s-0.5)^k
$$

在 $N = M+1$ 个**Gauss-Lobatto 节点** $\{s_j\}$ 处对力采样，得矩阵 $\mathbf{F}_p = [\mathbf{f}(s_1),\ldots,\mathbf{f}(s_N)]$。通过 Vandermonde 矩阵 $\mathbf{V}$（列为 $\mathbf{s}(s_j)$）的逆变换：

$$
\tilde{\mathbf{F}} = \mathbf{F}_p \cdot \mathbf{T}_p, \quad \mathbf{T}_p = \mathbf{V}^{-T}
$$

Gauss-Lobatto $N$ 点的积分精度为 $2N-3$ 阶，$N=M+1$ 时可达 $2M-1$ 阶，足以保证矩阵指数有理近似决定的精度阶次不受积分截断误差影响。

---

### 步骤 4：系数矩阵 $\mathbf{c}$（或 $\mathbf{c}_r$）的预计算

由矩阵多项式 $C_k = C_k(\mathbf{A})$ 的递推关系：

$$
C_0 = \mathbf{A}^{-1}(P - Q), \quad C_k = \mathbf{A}^{-1}\!\left(kC_{k-1} + \left(-\tfrac{1}{2}\right)^k(P - (-1)^kQ)\right)
$$

提取各阶多项式系数，得到标量系数矩阵 $\mathbf{c} = [c_{ki}]$（行 $k$：展开阶数 $0\sim p_f$；列 $i$：多项式次数 $0\sim M-1$）。对 M 格式，还需对 $\mathbf{A}$ 基底做 Horner 移位变换至 $\mathbf{A}_r$ 基底，得 $\mathbf{c}_r$。

**预计算量：** 以上系数仅依赖于 $M$ 和 $\rho_\infty$，在整个时间积分过程中**只需计算一次**。

---

### 步骤 5：时步推进求解（每个时步重复执行）

**Padé 方案（互异根）的时步算法：**

1. 对每个根 $r_i$（$i=1,\ldots,M$，共轭根只算一个）：
   - 计算合并力向量：$\mathbf{f}_i = \mathbf{F}_p\mathbf{T}_p\mathbf{c}\mathbf{r}_i$
   - 计算右端向量：$\mathbf{g}_i = P_L(r_i)\mathbf{z}_{n-1}$
   - 求解等效方程：$(r_i^2\mathbf{M} + r_i\Delta t\mathbf{C} + \Delta t^2\mathbf{K})\hat{\mathbf{x}}_i = r_i\mathbf{M}\hat{\mathbf{g}}_i - \Delta t^2\mathbf{K}\bar{\mathbf{g}}_i + r_i\Delta t^2\mathbf{f}_i$
   - 位移分量：$r_i\bar{\mathbf{x}}_i = \hat{\mathbf{x}}_i + \bar{\mathbf{g}}_i$
2. 合并：$\mathbf{z}_n = \rho\mathbf{z}_{n-1} + \sum_{i=1}^{M}a_i\mathbf{x}_i$（共轭根用 $2\mathrm{Re}(a_i\mathbf{x}_i)$）

**M 格式（单重实根）的时步算法：**

1. 计算 $M$ 个右端向量：$\mathbf{b}_i = p_{ri}\mathbf{z}_{n-1} + [\Delta t^2\mathbf{M}^{-1}\mathbf{f}_{ri},\ \mathbf{0}]^T$
2. Horner 递推（$i=1 \to M$，共用同一实等效刚度矩阵）：
   $$
   (r\mathbf{I} - \mathbf{A})\cdot\mathbf{x}_i = \mathbf{b}_{i-1} + \mathbf{x}_{i-1}
   $$
   等效刚度展开形式：$(r^2\mathbf{M} + r\Delta t\mathbf{C} + \Delta t^2\mathbf{K})\hat{\mathbf{x}}_i = r\mathbf{M}\hat{\mathbf{g}}_i - \Delta t^2\mathbf{K}\bar{\mathbf{g}}_i + r\Delta t^2\mathbf{f}_{ri}$
3. 最终：$\mathbf{z}_n = p_{rM}\mathbf{z}_{n-1} + \mathbf{x}_M$

---

### 步骤 6：加速度的向量运算计算（可选，无额外方程求解）

加速度 $\ddot{\mathbf{u}}_n = \overset{\circ}{\mathbf{u}}_n / \Delta t^2$ 由以下纯向量运算得到：

- **Padé 方案：** $\overset{\circ}{\mathbf{u}}_n = \rho\,\overset{\circ}{\mathbf{u}}_{n-1} + \sum_{i=1}^{M}a_i(r_i\hat{\mathbf{x}}_i - \overset{\circ}{\bar{\mathbf{g}}}_i)$
- **M 格式：** $\overset{\circ}{\mathbf{u}}_n = p_{rM}\overset{\circ}{\mathbf{u}}_{n-1} + (r\hat{\mathbf{x}}_M - \overset{\circ}{\bar{\mathbf{g}}}_M)$

这里 $\overset{\circ}{\bar{\mathbf{g}}}_i = \overset{\circ}{\bar{\mathbf{g}}}_{i-1}\cdot r_i + p_{Li}\,\overset{\circ}{\mathbf{u}}_{n-1}$，所有量均在步骤 5 中已得，**完全无需重解方程**。

---

### 步骤 7：非线性问题的处理（固定点迭代）

对非线性问题，$\mathbf{f}_{ri}$ 依赖于时步内各 Gauss-Lobatto 点处的未知状态 $\mathbf{z}(s_j)$。用 Hermite 多项式（利用时步两端的位移、速度、加速度，五次多项式，$p_{n-1}=p_n=2$）插值内部点状态：

$$
\mathbf{u}(t_{n-1}+s\Delta t) = \sum_{k=0}^{2}\alpha_k(s)\mathbf{u}_{n-1}^{(k)} + \sum_{k=0}^{2}\beta_k(s)\mathbf{u}_n^{(k)}
$$

插值误差为 $O(\Delta t^{p_{n-1}+p_n+3}) = O(\Delta t^7)$，因此非线性问题的最高可达精度阶次为 7 阶。对线性问题，力不依赖未知量，无需插值，精度仅由有理近似决定。

---

## 第四部分：论文的创新点

### 创新一：彻底消除质量矩阵分解需求

**具体实现：** 通过对非齐次项中的 $\mathbf{C}_k/\mathbf{Q}$ 进行部分分式展开，并**交换双重求和顺序**（先对 $k$ 求和再对 $i$ 求和），将原本含 $\mathbf{M}^{-1}$ 的力向量 $\Delta t^2\mathbf{M}^{-1}\tilde{\mathbf{f}}^{(k)}$ 转化为仅需直接采样外力值 $\mathbf{f}(s_j)$ 的合并向量 $\mathbf{f}_i$（不含 $\mathbf{M}^{-1}$）。右端项中最终保留的 $\mathbf{M}^{-1}$ 仅来自等效刚度矩阵方程，而该矩阵具有与梯形法则相同的稀疏结构，不需要单独对 $\mathbf{M}$ 分解。对非线性问题的意义尤为突出——旧算法每次迭代都需重新计算 $\mathbf{M}^{-1}\mathbf{f}$，新算法只需在 Gauss-Lobatto 点处更新 $\mathbf{f}$ 的采样值。

### 创新二：仅用向量运算获得与位移同阶的加速度

**具体实现：** 利用辅助方程关系 $r_i\bar{\mathbf{x}}_i = \hat{\mathbf{x}}_i + \bar{\mathbf{g}}_i$ 对 $s$ 求导，推导出 $\overset{\circ}{\hat{\mathbf{x}}}_i = r_i\hat{\mathbf{x}}_i - \overset{\circ}{\bar{\mathbf{g}}}_i$，其中右端量均已在时步推进过程中计算，仅需额外完成向量加减乘，**不引入任何方程求解**。精度可达与位移/速度完全相同的阶次（Padé 方案为 $2M$ 或 $2M-1$ 阶，M 格式为 $M$ 阶）。数值验证显示，该方法计算加速度与直接从运动方程求解的结果之间的 $L_\infty$ 范数相对差均小于 $10^{-8}$。

### 创新三：统一的算法框架同时适用于互异根与单重实根两种情形

**具体实现：** 两种情形的时步方程均可写成统一形式 $(r_i\mathbf{I}-\mathbf{A})\mathbf{x}_i = \mathbf{g}_i + [\Delta t^2\mathbf{M}^{-1}\mathbf{f}_{ri},\ \mathbf{0}]^T$，按相同流程求解。区别仅在于系数初始化：Padé 方案需要对 $M$ 个（可能复数）不同根分别建立等效刚度矩阵，而 M 格式 $M$ 步共用同一实等效刚度矩阵，只做一次 LU 分解，但精度阶次低一半。

### 创新四：非线性问题与线性问题相同的收敛阶次

**具体实现：** 通过选取足够多的 Gauss-Lobatto 采样点（$N = M+1$）保证力积分精度不低于时间步进精度，并用五次 Hermite 多项式插值时步内状态变量，使得非线性问题的截断误差阶次与线性问题一致。数值算例中，高非线性单摆问题的收敛率与线性单自由度系统完全一致，高阶格式使用比二阶格式大几个量级的时步仍能实现 $10^{-3}\%$ 以下的极高精度。

### 创新五：高效抑制波传播问题中的虚假高频振荡

**具体实现：** 高阶格式（$M \ge 3$）在使用较大 CFL 数（如 $M=5$ 时 $\mathrm{CFL}=12$）的前提下，有效滤除了空间离散引入的高频虚假模态，加速度响应质量远优于 HHT-$\alpha$ 方法（对 Lamb 问题 CFL=2 时仍有明显振荡）和显式中心差分法（加速度完全被污染至不可用）。对同一问题，高阶格式每步计算虽贵，但时步可大十倍以上，总计算时间反而更短（见 Table 3 的 Lamb 问题算例）。

---

## 补充说明：快速理解全文的要点

**最核心的一句话：** 本文通过对 $P(\mathbf{A})/Q(\mathbf{A})$ 中分母 $Q(\mathbf{A})$ 的部分分式展开，将原来需要"先算 $\mathbf{M}^{-1}$、再算时步"的两步流程变为"直接对力采样、时步与力向量操作分离"，使高阶自启动格式的全部优势在非线性问题中也能完全发挥。

**两种方案的选择建议：**

| 方案 | 精度阶次 | 等效刚度矩阵 | 适用场景 |
|------|----------|-------------|---------|
| Padé-PF（互异根） | $2M-1$（有耗散）/ $2M$（无耗散） | $M$ 个（复数） | 追求最高精度、问题规模适中 |
| M-PF（单重实根） | $M$ | 1 个（实数），共享 | 大规模问题、对实数求解器友好 |

**关于 $\rho_\infty$ 参数的物理意义：** $\rho_\infty = 1$ 时格式为 $A$ 稳定完全无耗散（适合精确捕捉高频响应）；$\rho_\infty = 0$ 时为 $L$ 稳定最强耗散（适合含高频虚假模态的波传播问题中过滤噪声）。实际应用中对含高频虚假模态的问题推荐取 $\rho_\infty = 0$，其他情形可取较大值以减少数值耗散对低频精度的影响。

**数值验证的代表性结论：**
1. 本文算法与前作 [9,36] 的算法结果数值完全一致，说明部分分式重构后精度等价。
2. 非线性单摆（接近翻转，$\theta_\mathrm{max} = 179.9°$）中，高阶格式 $M \ge 3$ 用 $\Delta t = T/200$ 即可与参考解重合，而低阶格式在第二个周期即产生明显漂移。
3. 二维 Lamb 问题（1568 万自由度）中，Padé-PF:23 使用 $\mathrm{CFL}=20$ 时，总计算时间仅为 HHT-$\alpha$（$\mathrm{CFL}=2$）的约 60%，而结果精度远优于后者。
