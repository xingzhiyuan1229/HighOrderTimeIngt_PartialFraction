# 论文总结报告

**论文标题：** A high-order implicit time integration method for linear and nonlinear dynamics with efficient computation of accelerations

**作者：** Daniel O'Shea, Xiaoran Zhang, Shayan Mohammadian, Chongmin Song（澳大利亚新南威尔士大学）

---

## 一、作者研究的问题

结构动力学和波传播问题的数值模拟依赖直接时间积分格式。工程中常用的隐式时间积分方法（Newmark、HHT-$[\alpha]$、广义-$[\alpha]$ 等）均为二阶精度，在高阶精度场景下存在以下核心缺陷：

1. **需要对质量矩阵进行分解：** 时间步进方程中包含 $[\Delta t^2 \mathbf{M}^{-1}\tilde{\mathbf{f}}^{(k)}]$ 项，必须先对质量矩阵做分解才能计算。对于非线性问题，外力 $[\mathbf{f}]$ 在当前时间步内未知，每次迭代都需重新计算 $[\mathbf{M}^{-1}\mathbf{f}]$，计算代价高昂。
2. **加速度精度低或需额外方程求解：** 自启动算法计算加速度时，若要保持与位移、速度相同的精度阶次，传统做法需要额外求解一次含质量矩阵的方程组，失去了自启动算法的计算优势。
3. **非线性问题收敛性未充分验证：** 前期工作（[9][36]）仅在线性问题上验证了高阶方案的性能，尚未证明其对强非线性问题同样适用。

作者在前期工作 [9][36] 的基础上，针对上述三个问题提出了新算法。

---

## 二、作者的主要工作与创新点

### 创新点一：通过部分分式展开消除 $[\mathbf{M}^{-1}\mathbf{f}]$ 项

#### 数学推导

原时间步进方程（来自用矩阵指数有理逼近后的 Eq.22）：

$$
\mathbf{z}_n = \frac{\mathbf{P}}{\mathbf{Q}}\mathbf{z}_{n-1} + \frac{1}{\mathbf{Q}}\sum_{k=0}^{p_f}\mathbf{C}_k\begin{Bmatrix}\Delta t^2\mathbf{M}^{-1}\tilde{\mathbf{f}}^{(k)}\\\mathbf{0}\end{Bmatrix}
$$

右端显式出现 $[\mathbf{M}^{-1}\tilde{\mathbf{f}}^{(k)}]$，必须先分解质量矩阵。

**部分分式的关键步骤：** 将 $[\mathbf{P}/\mathbf{Q}]$ 和 $[\mathbf{C}_k/\mathbf{Q}]$ 分别按分母多项式 $[Q]$ 的根 $[r_i]$ 做部分分式展开，将对 $k$ 的求和提到矩阵求逆之前，使外力先完成标量加权组合，再参与矩阵运算：

$$
\frac{1}{\mathbf{Q}}\sum_{k=0}^{p_f}\mathbf{C}_k\begin{Bmatrix}\Delta t^2\mathbf{M}^{-1}\tilde{\mathbf{f}}^{(k)}\\\mathbf{0}\end{Bmatrix}
= \sum_{i=1}^{M}\frac{a_i}{r_i\mathbf{I}-\mathbf{A}}\begin{Bmatrix}\Delta t^2\mathbf{M}^{-1}\mathbf{f}_i\\\mathbf{0}\end{Bmatrix}
$$

其中 $[\mathbf{f}_i = \sum_{k=0}^{p_f}\tilde{\mathbf{f}}^{(k)}C_k(r_i)]$ 是标量加权后的力向量，无 $[\mathbf{M}^{-1}]$。

展开隐式方程 $[(r_i\mathbf{I}-\mathbf{A})\mathbf{x}_i = \cdots]$，利用 $[\mathbf{A}]$ 的分块结构，其上半部分（速度分量 $[\hat{\mathbf{x}}_i]$）满足（Eq.73）：

$$
\left(r_i^2\mathbf{M} + r_i\Delta t\mathbf{C} + \Delta t^2\mathbf{K}\right)\hat{\mathbf{x}}_i = r_i\mathbf{M}\hat{\mathbf{g}}_i - \Delta t^2\mathbf{K}\bar{\mathbf{g}}_i + r_i\Delta t^2\mathbf{f}_i
$$

右端变为 $[r_i\Delta t^2\mathbf{f}_i]$ 而不是 $[r_i\mathbf{M}(\Delta t^2\mathbf{M}^{-1}\mathbf{f}_i)]$，**质量矩阵求逆被彻底消除**。最终时间步进方程化简为：

$$
\mathbf{z}_n = \rho\,\mathbf{z}_{n-1} + \sum_{i=1}^{M} a_i\,\mathbf{x}_i
$$

#### 代码实现（`InitSchemePadePF.m` + `TimeSolverPF.m`）

**第一步：初始化阶段预计算所有与根相关的系数（`InitSchemePadePF.m`）**

```matlab
function [rho, plr, rs, a, cfr] = InitSchemePadePF(M, rhoInfty, pf)
    [pcoe, qcoe, rs] = PadeExpansion(M, rhoInfty);  % 得到 P、Q 的系数和 Q 的根 r_i
    rho  = pcoe(end)/qcoe(end);                     % ρ = p_M/q_M（高频谱半径相关）
    a    = polyPartialFraction(qcoe, rs);            % 计算各根的部分分式系数 a_i（Eq.34）
    plcoe = pcoe(1:end-1) - rho*qcoe(1:end-1);      % P_L 的系数（去掉最高阶后的余项）
    plr  = plcoe * reshape(rs,1,[]).^((0:M-1)');     % 对每个根求 P_L(r_i)（Eq.37）
    cf   = TimeIntgCoeffForce(pcoe, qcoe, pf);       % 矩阵 c = [c_{ki}]（Eq.26）
    cfr  = cf * reshape(rs,1,[]).^((0:M-1)');        % 对每个根求 C_k(r_i)（Eq.38）
end
```

- `polyPartialFraction`（`polyPartialFraction.m`）实现 $[a_i = 1/\prod_{j\ne i}(r_j - r_i)]$：

  ```matlab
  allRoots = [r; conj(r(nReal+1:end))];          % 补全复共轭根
  a(ii) = 1/prod(allRoots([1:ii-1,ii+1:end]) - r(ii));  % Eq.(34)
  ```

- `TimeIntgCoeffForce`（`TimeIntgCoeffForce.m`）递推计算系数矩阵 $[\mathbf{c}=[c_{ki}]]$（Eq.24/25）：

  ```matlab
  C(1,:) = (p - q)(2:end);                              % k=0 的初始值 C_0
  for k = 1:pf
      tmp = ((-1/2)^k)*(p - (-1)^k * q);
      tmp(1:M) = tmp(1:M) + k*C(k,:);                   % 递推关系 Eq.(24)
      C(k+1,:) = tmp(2:end);
  end
  ```

**第二步：预计算合并变换矩阵 `Tcfr`（在时间循环外，仅一次）**

```matlab
s    = forceSamplingPoints(N);               % Gauss-Lobatto 采样点 s_1,...,s_N
Tcfr = transMtxPointsToPoly(s, p+1) * cfr(1:p+1,:);
%  transMtxPointsToPoly 计算 T_p = V^{-T}（Eq.83），cfr 存放 C_k(r_i)
%  因此 Tcfr[:,i] = T_p * c * r_i，即 Eq.(81) 中从采样力到 f_i 的映射
```

**第三步：时间循环——直接用矩阵-向量乘积得到 $[\mathbf{f}_i]$，无需 $[\mathbf{M}^{-1}]$**

```matlab
Fp  = F .* reshape(signal(ts), 1, []);    % F_p：各采样点的物理力列向量，已乘 Δt²
fri = Fp * real(Tcfr(:,1));              % f_i = F_p * T_p * c * r_i（Eq.81）
% 右端组装：r²M*ĝ_i - K*ḡ_i + r*f_i  ← f_i 直接参与，不出现 M⁻¹
ftmp = dKd1 \ (r*(M*g(1:n)) - K*g(n+1:end) + r*fri);
```

对比旧算法（需要 $[\mathbf{M}^{-1}\mathbf{f}]$）：  
旧算法右端应为 `r*M*(dt²*M\f_i)` = 先算 `M\f_i` 再乘 `M`，等价于 `r*dt²*f_i`，但在代码层面必须单独存储并计算 `M\f_i`。新算法把 `M⁻¹` 代数消去，直接用 `r*fri`，**无需任何额外的质量矩阵分解步骤**。

**预分解有效刚度矩阵（仅一次，供所有时间步复用）：**

```matlab
% 实根（只有一个）：使用 decomposition 对象
dKd1 = decomposition(sparse((r*r)*M + r*C + K));

% 复数根（成对出现）：使用 LU 分解（复数矩阵，比 decomposition 更快）
[L{ic},U{ic},LUp{ic},LUq{ic}] = lu(sparse((r*r)*M + r*C + K), 'vector');
```

注意：有效刚度矩阵 $[r_i^2\mathbf{M} + r_i\Delta t\mathbf{C} + \Delta t^2\mathbf{K}]$ 的稀疏性与静力刚度矩阵完全相同，只分解一次即可供所有时间步重复使用，这是该格式高效的关键。

---

### 创新点二：仅用向量运算高精度计算加速度

#### 数学推导

状态向量 $[\mathbf{z} = \{\dot{\mathbf{u}};\,\mathbf{u}\}]$（无量纲）中，上半部分恰好是下半部分对无量纲时间 $[s]$ 的导数。因此辅助变量 $[\mathbf{x}_i = \{\hat{\mathbf{x}}_i;\,\bar{\mathbf{x}}_i\}]$ 中，$[\hat{\mathbf{x}}_i]$ 就是 $[\bar{\mathbf{x}}_i]$ 的无量纲时间导数，即 $[\hat{\mathbf{x}}_i = \partial\bar{\mathbf{x}}_i/\partial s]$。

由 Eq.(74)：$[\bar{\mathbf{x}}_i = (\hat{\mathbf{x}}_i + \bar{\mathbf{g}}_i)/r_i]$，对 $[s]$ 求导：

$$
\frac{\partial\bar{\mathbf{x}}_i}{\partial s} = \frac{1}{r_i}\!\left(\frac{\partial\hat{\mathbf{x}}_i}{\partial s} + \frac{\partial\bar{\mathbf{g}}_i}{\partial s}\right)
$$

左端即为 $[\hat{\mathbf{x}}_i]$，整理得：

$$
\frac{\partial\hat{\mathbf{x}}_i}{\partial s} = r_i\hat{\mathbf{x}}_i - \frac{\partial\bar{\mathbf{g}}_i}{\partial s}
$$

对于不同根情形，$[\bar{\mathbf{g}}_i = P_L(r_i)\bar{\mathbf{z}}_{n-1}]$（下半部分），其无量纲时间导数为 $[P_L(r_i)\hat{\mathbf{z}}_{n-1} = \hat{\mathbf{g}}_i]$（上半部分）。故：

$$
\frac{\partial\hat{\mathbf{x}}_i}{\partial s} = r_i\hat{\mathbf{x}}_i - \hat{\mathbf{g}}_i
$$

因此无量纲加速度（Eq.77）为：

$$
\overset{\circ}{\mathbf{u}}_n = \rho\,\overset{\circ}{\mathbf{u}}_{n-1} + \sum_{i=1}^{M}a_i\!\left(r_i\hat{\mathbf{x}}_i - \hat{\mathbf{g}}_i\right)
$$

**关键：$[\hat{\mathbf{x}}_i]$ 和 $[\hat{\mathbf{g}}_i]$ 均已在求解 $[\mathbf{x}_i]$ 时得到，不需要任何额外的方程求解。**

#### 代码实现（`TimeSolverPF.m`）

```matlab
% --- 在每个时间步开始时初始化累计加速度 ---
an = rho * a0;                   % ρ·ü_{n-1}（无量纲加速度乘以 Δt²）

% --- 实根分支 ---
r = real(rs(1));
g = real(plr(1)) * z0;           % g_i = P_L(r_i) * z_{n-1}，上半 ĝ_i = g(1:n)
fri  = Fp * real(Tcfr(:,1));
ftmp = dKd1 \ (r*(M*g(1:n)) - K*g(n+1:end) + r*fri);  % 解得 x̂_i = ftmp
x    = [ftmp; (ftmp + g(n+1:end))/r];                  % 完整 x_i

z  = z  + real(a(1)) * x;                       % Eq.(45) 位移/速度更新
an = an + real(a(1)) * (r*x(1:n) - g(1:n));    % Eq.(77) 加速度更新
%                      ↑──────────────────── r·x̂_i - ĝ_i，纯向量运算！

% --- 复数根分支（类似，取两倍实部）---
for ic = 1:nCmplx
    r  = rs(ic+nReal);
    cg = plr(ic+nReal) * z0;                    % 复数 g_i，上半为 ĉg_i
    cfri = Fp * Tcfr(:,ic+nReal);
    ctmp_rhs = plr(ic+nReal)*(r*(M*z0(1:n)) - K*z0(n+1:end)) + r*cfri;
    tmp(LUp{ic},:) = U{ic} \ (L{ic} \ ctmp_rhs(LUq{ic},:));  % 解复数方程，得 x̂_i
    x  = [tmp; (tmp + cg(n+1:end))/r];
    z  = z  + 2*real(a(ic+nReal) * x);
    an = an + 2*real(a(ic+nReal) * (r*tmp - cg(1:n)));  % r·x̂_i - ĝ_i，取实部
end
```

对于**单重根情形**（`TimeSolverTRhoPF.m`），最后一次迭代结束后的 `x(1:n)` 即为 $[\hat{\mathbf{x}}_M]`，`g(1:n)` 即为 $[\hat{\mathbf{g}}_M]$：

```matlab
for ip = 1:p
    g = x + prcoe(ip)*z;                      % g_i = x_{i-1} + p_{ri}·z_{n-1}
    rfri = Fp * Tcfr1(:,ip);
    x(1:n) = dKd \ (r*(M*g(1:n)) - K*g(n+1:end) + rfri);
    x(n+1:end) = (x(1:n) + g(n+1:end)) / r;
end
z  = prcoe(p+1)*z + x;
an = prcoe(end)*an + (r*x(1:n) - g(1:n));   % p_{rM}·ü_{n-1} + (r·x̂_M - ĝ_M)
%                     ↑─────────────────── 只用循环末尾已有的 x̂_M 和 ĝ_M，零额外代价
```

最终还原物理量纲：

```matlab
vel = vel / dt;        % Δt·u̇ → u̇
acc = acc / (dt*dt);   % Δt²·ü → ü
```

---

### 创新点三：Gauss-Lobatto 采样 + 预合并变换矩阵处理非均匀项

#### 数学推导

力向量 $[\mathbf{f}(s)]$ 在时间步内展开为 Taylor 多项式（Eq.14）：

$$
\mathbf{f}(s) = \sum_{k=0}^{p_f}\tilde{\mathbf{f}}^{(k)}(s-0.5)^k = \tilde{\mathbf{F}}\cdot\mathbf{s}(s)
$$

在 $[N = M+1]$ 个 Gauss-Lobatto 采样点 $[s_1,\ldots,s_N]$ 处采样，构造矩阵方程（Eq.80）：

$$
\mathbf{F}_p = \tilde{\mathbf{F}}\cdot\mathbf{V}, \quad \mathbf{V}_{kj} = (s_k - 0.5)^j
$$

由此反解展开系数（Eq.83）：

$$
\tilde{\mathbf{F}} = \mathbf{F}_p\cdot\mathbf{T}_p,\quad \mathbf{T}_p = \mathbf{V}^{-T}
$$

则对于不同根情形，各子步的外力 $[\mathbf{f}_i]$ 表示为（Eq.81）：

$$
\mathbf{f}_i = \tilde{\mathbf{F}}\cdot\mathbf{c}\cdot\mathbf{r}_i = \mathbf{F}_p\cdot(\mathbf{T}_p\cdot\mathbf{c}\cdot\mathbf{r}_i)
$$

括号内 $[\mathbf{T}_p\cdot\mathbf{c}\cdot\mathbf{r}_i]$ 与时间无关，可在时间循环外预计算为列向量，时间循环内仅需一次矩阵-向量乘积。

#### 代码实现

**`transMtxPointsToPoly.m`——计算 $[\mathbf{T}_p = \mathbf{V}^{-T}]$：**

```matlab
a = (s - 0.5).^(0:nf-1);   % 构造 V^T，行对应采样点，列对应多项式阶次
T = a / (a'*a);             % T_p = V^{-T}（最小二乘逆，采样点>=多项式阶时精确）
```

**`forceSamplingPoints.m`——生成 Gauss-Lobatto 点：**

```matlab
xi = lglnodes(np-1);        % Legendre-Gauss-Lobatto 节点（-1到1）
xi = flip(xi);
s  = 1/2*(scl*xi + 1);      % 映射到 [0,1]，scl 略小于 1 避免端点数值奇异
```

**初始化时预合并（仅一次）：**

```matlab
% 不同根情形（InitSchemePadePF.m → TimeSolverPF.m）：
cf   = TimeIntgCoeffForce(pcoe, qcoe, pf);       % 矩阵 c，大小 (pf+1) × M
cfr  = cf * reshape(rs,1,[]).^((0:M-1)');        % 对每个根 r_i 求 C_k(r_i)，大小 (pf+1)×M
Tcfr = transMtxPointsToPoly(s, p+1) * cfr(1:p+1,:);  % T_p * cfr，大小 N×M
% Tcfr(:,i) = T_p·c·r_i  ← 第 i 根对应的列向量

% 单重根情形（InitSchemeRhoPF.m → TimeSolverTRhoPF.m）：
cfr1  = r * shiftPolyCoefficients(cf, r);        % r·c_{ri}，移位后再乘 r
Tcfr1 = transMtxPointsToPoly(s, p+1) * cfr1(1:p+1,:);  % T_p * (r·c_r)
```

注意单重根情形中 `cfr1` 已把因子 $[r]$ 吸收进去，使时间循环内直接得到 $[r\mathbf{f}_{ri}]$：

```matlab
rfri = Fp * Tcfr1(:,ip);   % 直接得到 r·f_{ri}，右端方程无需额外乘 r
```

**时间循环内采样并使用（`TimeSolverPF.m`）：**

```matlab
ts = tm + dt*s;                              % 各采样时刻（物理时间）
Fp = F .* reshape(signal(ts), 1, []);        % 各采样点力值，大小 n×N
fri = Fp * real(Tcfr(:,1));                 % f_i = F_p * Tcfr[:,i]，大小 n×1
```

整个处理链路为：采样点 $[s_k]$ → 物理时刻 $[t_k]$ → 调用 `signal(ts)` 得到载荷幅值 → 与空间力分布 `F` 相乘得到 $[\mathbf{F}_p]$ → 乘预合并矩阵 `Tcfr` 得到 $[\mathbf{f}_i]$，**每步仅需一次矩阵-向量乘积**。

---

### 创新点四：首次系统验证强非线性问题收敛性

以简单摆（强非线性 SDOF）和三自由度非线性弹簧系统为算例，证明：在采用 Hermite 内插时，非线性问题的收敛阶次与线性问题相同（最高可达7阶），在同等精度需求下可使用比二阶方法大若干数量级的时间步长。

非线性问题中，内插函数 `signal` 在每步迭代中根据当前对端点状态的估计（利用位移、速度、加速度做五次 Hermite 插值）动态更新，形成固定点迭代直至收敛。此部分的精度上限为7阶（由五次 Hermite 多项式截断误差 $[O(\Delta t^7)]$ 决定），对线性问题无此限制。

---

## 三、论文大致流程与关键公式-代码串联

### 3.1 整体流程概览

```
用户输入: M (近似阶次), ρ∞ (耗散参数), Δt, K, M_mat, C, F, u0, v0
           ↓
【初始化阶段，仅执行一次】
  InitSchemePadePF(M, ρ∞, pf)  或  InitSchemeRhoPF(M, ρ∞, pf)
    ├─ PadeExpansion / MschemeRoot+pCoefficients → 多项式系数 pcoe, qcoe
    ├─ polyPartialFraction(qcoe, rs)             → 部分分式系数 a_i（创新点一）
    ├─ P_L(r_i) 对各根求值                       → plr（辅助向量系数）
    └─ TimeIntgCoeffForce + 对根求值             → cfr（力加权系数）
           ↓
  forceSamplingPoints(N)                         → Gauss-Lobatto 点 s（创新点三）
  transMtxPointsToPoly(s, N) * cfr              → Tcfr（预合并变换矩阵）
           ↓
  预分解有效刚度矩阵 r²M + rΔtC + Δt²K         → dKd / L,U（仅分解一次）
           ↓
【时间循环】for it = 2:ns
  ts = tm + dt*s  →  Fp = F .* signal(ts)       → 采样外力（创新点三）
  z  = ρ·z_{n-1}，an = ρ·a_{n-1}               → 高频耗散初始化
  对每个根 r_i：
    g_i  = P_L(r_i)·z_{n-1}                     → 辅助向量
    f_i  = Fp * Tcfr[:,i]                        → 外力加权（创新点一/三：无 M⁻¹）
    解 (r²M+rC+K)·x̂_i = rMĝ_i - Kḡ_i + r·f_i → 有效刚度方程
    x̄_i = (x̂_i + ḡ_i) / r_i
    z   += a_i · x_i                             → 状态更新（Eq.45）
    an  += a_i · (r_i·x̂_i - ĝ_i)              → 加速度更新（创新点二：纯向量）
  输出 dsp, vel, acc（还原量纲）
```

### 3.2 关键公式与代码逐一对应

#### 公式 (10)：状态空间一阶 ODE

$$
\frac{\mathrm{d}\mathbf{z}}{\mathrm{d}s} = \mathbf{A}\mathbf{z}(s) + \bar{\mathbf{f}}(\mathbf{z}(s)),\quad
\mathbf{A} = \begin{bmatrix}-\Delta t\mathbf{M}^{-1}\mathbf{C} & -\Delta t^2\mathbf{M}^{-1}\mathbf{K}\\\mathbf{I} & \mathbf{0}\end{bmatrix}
$$

**代码：** $[\mathbf{A}]$ 不显式构造，其作用通过对角块分别操作实现。预处理时缩放矩阵使方程无量纲化：

```matlab
K  = dt*dt*sparse(K);    % Δt²K（下标缩写省去 Δt²）
C  = dt*sparse(C);       % ΔtC
v0 = dt*v0;              % 无量纲速度 = Δt·u̇₀
z0 = [v0; u0];           % z₀ = {Δt·u̇₀; u₀}
```

---

#### 公式 (21)：矩阵指数的有理逼近（Padé 展开）

$$
e^{\mathbf{A}} \approx \frac{P(\mathbf{A})}{Q(\mathbf{A})} = \frac{p_0\mathbf{I}+\cdots+p_M\mathbf{A}^M}{q_0\mathbf{I}+\cdots+q_M\mathbf{A}^M}
$$

**代码（`InitSchemePadePF.m`）：**

```matlab
[pcoe, qcoe, rs] = PadeExpansion(M, rhoInfty);
% pcoe = [p0,...,pM]，qcoe = [q0,...,qM]，rs = Q(x) 的根
rho = pcoe(end)/qcoe(end);   % ρ = p_M/q_M
```

对单重根情形（`InitSchemeRhoPF.m`）：

```matlab
r     = MschemeRoot(M, rhoInfty);   % 求单重根
pcoe  = pCoefficients(M, r);        % P(A) 的系数
prcoe = shiftPolyCoefficients(pcoe, r);  % 移位到 A_r = rI-A 的系数
```

---

#### 公式 (34)(36)：部分分式系数与 $[\mathbf{P}/\mathbf{Q}]$ 展开（**创新点一的核心**）

$$
a_i = \frac{1}{\prod_{j\ne i}(r_j - r_i)},\quad
\frac{\mathbf{P}}{\mathbf{Q}} = \rho\mathbf{I} + \sum_{i=1}^{M}\frac{a_i\,P_L(r_i)}{r_i\mathbf{I}-\mathbf{A}}
$$

**代码（`polyPartialFraction.m`）：**

```matlab
allRoots = [r; conj(r(nReal+1:end))];              % 补全共轭根（复根成对）
for ii = 1:nterm
    a(ii) = 1/prod(allRoots([1:ii-1,ii+1:end]) - r(ii));  % Eq.(34)
end
```

**代码（`InitSchemePadePF.m`）：**

```matlab
plcoe = pcoe(1:end-1) - rho*qcoe(1:end-1);         % P_L 系数（降阶后余项）
plr   = plcoe * reshape(rs,1,[]).^((0:M-1)');       % 对每个根 r_i 求 P_L(r_i)
```

时间循环中用 `plr` 得到辅助向量：

```matlab
g = real(plr(1)) * z0;     % g_i = P_L(r_i)·z_{n-1}（实根）
cg = plr(ic+nReal) * z0;   % g_i = P_L(r_i)·z_{n-1}（复根，保留复数形式）
```

---

#### 公式 (81)：外力加权组合（**创新点三的核心**）

$$
\mathbf{f}_i = \mathbf{F}_p\,\mathbf{T}_p\,\mathbf{c}\,\mathbf{r}_i
$$

**代码（初始化阶段，时间循环外）：**

```matlab
cf   = TimeIntgCoeffForce(pcoe, qcoe, pf);          % c 矩阵，大小 (pf+1)×M
cfr  = cf * reshape(rs,1,[]).^((0:M-1)');           % C_k(r_i) = c * r_i^j
Tcfr = transMtxPointsToPoly(s, p+1) * cfr(1:p+1,:);% T_p * cfr，预合并，大小 N×M
```

**代码（时间循环内）：**

```matlab
ts  = tm + dt*s;
Fp  = F .* reshape(signal(ts), 1, []);  % 大小 n×N（n=DOF数，N=采样点数）
fri = Fp * real(Tcfr(:,1));            % f_i：大小 n×1，仅一次矩阵-向量乘积
```

---

#### 公式 (73)：有效刚度方程求解（**创新点一的落地**）

$$
\underbrace{\left(r_i^2\mathbf{M} + r_i\Delta t\mathbf{C} + \Delta t^2\mathbf{K}\right)}_{\text{预分解，仅一次}}\hat{\mathbf{x}}_i
= r_i\mathbf{M}\hat{\mathbf{g}}_i - \Delta t^2\mathbf{K}\bar{\mathbf{g}}_i + r_i\Delta t^2\mathbf{f}_i
$$

**代码：**

```matlab
% 预分解（时间循环外）：
dKd1 = decomposition(sparse((r*r)*M + r*C + K));    % 实根：直接因子分解

% 时间循环内（实根）：
ftmp = dKd1 \ (r*(M*g(1:n)) - K*g(n+1:end) + r*fri);  % 解方程得 x̂_i
x    = [ftmp; (ftmp + g(n+1:end))/r];                  % 补全 x̄_i = (x̂_i+ḡ_i)/r

% 时间循环内（复根）：
ctmp = plr(ic+nReal)*(r*(M*z0(1:n)) - K*z0(n+1:end)) + r*cfri;  % 右端（直接用 f_i，无 M⁻¹）
tmp(LUp{ic},:) = U{ic} \ (L{ic} \ ctmp(LUq{ic},:));              % 复数 LU 求解
```

---

#### 公式 (45)(67)：时间步进状态更新

不同根情形（Eq.45）：

$$
\mathbf{z}_n = \rho\,\mathbf{z}_{n-1} + \sum_{i=1}^{M} a_i\,\mathbf{x}_i
$$

**代码（`TimeSolverPF.m`）：**

```matlab
z  = rho*z0;                           % ρ·z_{n-1}
z  = z + real(a(1))*x;                % 实根贡献
z  = z + 2*real(a(ic+nReal)*x);       % 每对复根贡献（共轭对取 2×实部）
z0 = z;
```

单重根情形（Eq.67），通过递推 Horner 格式：

$$
\mathbf{z}_n = p_{rM}\,\mathbf{z}_{n-1} + \mathbf{x}_M
$$

**代码（`TimeSolverTRhoPF.m`）：**

```matlab
x = zeros(2*n,1);
for ip = 1:p
    g = x + prcoe(ip)*z;                           % Horner 递推
    ...
    x(1:n)     = dKd \ (...);
    x(n+1:end) = (x(1:n) + g(n+1:end)) / r;
end
z = prcoe(p+1)*z + x;                             % z_n = p_{rM}·z_{n-1} + x_M
```

---

#### 公式 (77)(78)：加速度向量更新（**创新点二的落地**）

不同根：$[\overset{\circ}{\mathbf{u}}_n = \rho\,\overset{\circ}{\mathbf{u}}_{n-1} + \sum_{i}a_i(r_i\hat{\mathbf{x}}_i - \hat{\mathbf{g}}_i)]$

单重根：$[\overset{\circ}{\mathbf{u}}_n = p_{rM}\,\overset{\circ}{\mathbf{u}}_{n-1} + (r\hat{\mathbf{x}}_M - \hat{\mathbf{g}}_M)]$

**代码（`TimeSolverPF.m`）：**

```matlab
an = rho*a0;                                       % ρ·ü_{n-1}
an = an + real(a(1))*(r*x(1:n) - g(1:n));         % a_i·(r·x̂_i - ĝ_i) 实根
an = an + 2*real(a(ic+nReal)*(r*tmp - cg(1:n)));  % 复根对，注意 tmp = x̂_i，cg(1:n) = ĝ_i
```

**代码（`TimeSolverTRhoPF.m`）：**

```matlab
an = prcoe(end)*an + (r*x(1:n) - g(1:n));
%                   ↑─────────────────── g 是最后一次 Horner 迭代的 g_M，x(1:n)=x̂_M
```

**以上加速度更新全程只有向量加法和标量乘法，没有任何矩阵方程求解。**

---

## 四、数据流总结（从输入到输出）

| 阶段 | 操作 | 涉及函数/变量 | 对应创新点 |
|------|------|--------------|-----------|
| **初始化** | 计算 Padé/M-格式系数、根、部分分式系数 $[a_i]$ | `InitSchemePadePF` / `InitSchemeRhoPF` | 创新点一 |
| **采样点** | 生成 Gauss-Lobatto 点 $[s_k\in[0,1]]$ | `forceSamplingPoints` | 创新点三 |
| **变换矩阵** | 计算 $[\mathbf{T}_p = \mathbf{V}^{-T}]$ 并与 $[\mathbf{c}\cdot\mathbf{r}_i]$ 预合并为 `Tcfr` | `transMtxPointsToPoly` | 创新点三 |
| **预分解** | 对有效刚度矩阵 $[r_i^2\mathbf{M}+r_i\mathbf{C}+\mathbf{K}]$ 仅分解一次 | MATLAB `decomposition`/`lu` | 创新点一 |
| **时间循环** | 采样外力 → 矩阵-向量乘得 $[\mathbf{f}_i]$ → 求解 $[\hat{\mathbf{x}}_i]$ → 叠加更新 $[\mathbf{z}_n]$ | `TimeSolverPF` / `TimeSolverTRhoPF` | 创新点一、二、三 |
| **加速度** | 用已有中间变量做向量更新 $[\dot{\mathbf{u}}_n]$ | 同上，`an` 变量 | 创新点二 |
| **输出** | 还原量纲，存储位移 `dsp`、速度 `vel`、加速度 `acc` | — | — |

---

## 五、小结

本文的三个核心创新均在代码中有清晰落地：

- **创新点一（消除 $[\mathbf{M}^{-1}\mathbf{f}]$）** 体现在：`InitSchemePadePF` 预计算 `cfr = C_k(r_i)`，`transMtxPointsToPoly` 预合并变换矩阵 `Tcfr`，时间循环内仅用 `Fp * Tcfr(:,i)` 得到 $[\mathbf{f}_i]$，右端方程中是 `r*fri` 而非 `r*M*(M\fri)`，质量矩阵求逆被代数消去。

- **创新点二（向量更新加速度）** 体现在：求解 $[\hat{\mathbf{x}}_i]$ 的步骤兼顾了后续加速度的需要，`an += a_i*(r*x̂_i - ĝ_i)` 直接复用已有变量，全程无额外方程求解。

- **创新点三（高效外力积分）** 体现在：`forceSamplingPoints` 生成 Gauss-Lobatto 点，`Tcfr` 在时间循环外预计算，循环内仅一次矩阵-向量乘积完成力多项式的采样-插值-加权全流程。

三者协同使得整个高阶时间积分格式与传统二阶方法（如梯形规则）具有相同的有效刚度矩阵稀疏结构，仅需一次预分解，兼顾了高阶精度、可控数值耗散与大规模工程问题的计算效率。
