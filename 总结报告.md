# 论文总结报告

**论文标题：** A high-order implicit time integration method for linear and nonlinear dynamics with efficient computation of accelerations

**作者：** Daniel O'Shea, Xiaoran Zhang, Shayan Mohammadian, Chongmin Song（澳大利亚新南威尔士大学）

---

## 一、作者研究的问题

结构动力学和波传播问题的数值模拟依赖直接时间积分格式。工程中常用的隐式时间积分方法（Newmark、HHT-$[\alpha]$、广义-$[\alpha]$ 等）均为二阶精度，在高阶精度场景下存在以下核心缺陷：

1. **需要对质量矩阵进行分解：** 传统算法在每一时间步需要计算 $[\mathbf{M}^{-1}\mathbf{f}]$（质量矩阵求逆乘以外力），对于非线性问题每次迭代均需重复此操作，计算代价高昂。
2. **加速度精度低或需额外方程求解：** 高阶自启动格式计算加速度时，若要保持与位移、速度相同的精度阶次，需额外求解方程组，失去了自启动算法的计算优势。
3. **非线性问题收敛性未充分验证：** 前期工作（[9][36]）仅在线性问题上验证了高阶方案的性能，尚未证明其对强非线性问题同样适用。

作者在前期工作 [9][36] 的基础上，针对上述三个问题提出了新算法。

---

## 二、作者的主要工作与创新点

### 创新点一：通过部分分式展开消除 $[\mathbf{M}^{-1}\mathbf{f}]$ 项

**问题根源：** 时间步进方程（Eq. 22）中包含 $[\Delta t^2 \mathbf{M}^{-1}\tilde{\mathbf{f}}^{(k)}]$ 项，非线性问题中 $[\mathbf{f}]$ 在当前时间步未知，导致迭代求解时反复分解质量矩阵。

**创新做法：** 对有理逼近 $[\mathbf{P}/\mathbf{Q}]$ 及 $[\mathbf{C}_k/\mathbf{Q}]$ 做**部分分式展开**，将双重求和的顺序对调，使外力向量的组合在矩阵-向量乘法**之前**完成。最终时间步进方程被化简为：

$$
\mathbf{z}_n = \rho\,\mathbf{z}_{n-1} + \sum_{i=1}^{M} a_i\,\mathbf{x}_i
$$

其中每个子步 $[\mathbf{x}_i]$ 满足隐式方程：

$$
(r_i^2\mathbf{M} + r_i\Delta t\mathbf{C} + \Delta t^2\mathbf{K})\,\hat{\mathbf{x}}_i = r_i\mathbf{M}\hat{\mathbf{g}}_i - \Delta t^2\mathbf{K}\bar{\mathbf{g}}_i + r_i\Delta t^2\mathbf{f}_i
$$

右端的 $[\mathbf{f}_i]$ 是外力在 Gauss-Lobatto 采样点处的加权组合（见创新点三），**不再包含 $[\mathbf{M}^{-1}]$**，质量矩阵分解完全不需要。

### 创新点二：仅用向量运算高精度计算加速度

**问题根源：** 对时间步进函数求导即可得到加速度，但需要额外计算 $[\overset{\circ}{\hat{\mathbf{x}}}_i]$（辅助变量的维度时导数），这在原先算法中需要额外求解方程组。

**创新做法：** 对 $[\bar{\mathbf{x}}_i = (\hat{\mathbf{x}}_i + \bar{\mathbf{g}}_i)/r_i]$ 再次求维度导数，发现：

$$
\overset{\circ}{\hat{\mathbf{x}}}_i = r_i\hat{\mathbf{x}}_i - \overset{\circ}{\bar{\mathbf{g}}}_i
$$

右端 $[\hat{\mathbf{x}}_i]$ 和 $[\overset{\circ}{\bar{\mathbf{g}}}_i]$ 均已在时间步进过程中得出，**无需任何额外方程求解**。对于不同根情形，加速度更新公式为：

$$
\overset{\circ}{\mathbf{u}}_n = \rho\,\overset{\circ}{\mathbf{u}}_{n-1} + \sum_{i=1}^{M} a_i\!\left(r_i\hat{\mathbf{x}}_i - \overset{\circ}{\bar{\mathbf{g}}}_i\right)
$$

对于单重根情形：

$$
\overset{\circ}{\mathbf{u}}_n = p_{rM}\,\overset{\circ}{\mathbf{u}}_{n-1} + \left(r\hat{\mathbf{x}}_M - \overset{\circ}{\bar{\mathbf{g}}}_M\right)
$$

加速度精度与位移、速度完全一致，且仅需向量运算。

### 创新点三：非均匀项（外力）的数值积分策略

力向量 $[\mathbf{f}]$ 在时间步内展开为关于无量纲时间中点的 Taylor 多项式（Eq. 14）：

$$
\mathbf{f}(s) = \sum_{k=0}^{p_f}\tilde{\mathbf{f}}^{(k)}(s-0.5)^k
$$

展开系数由在 **Gauss-Lobatto 积分点** $[\bar{s}_k]$ 处采样的力值反算得到（Eq. 83）：

$$
\tilde{\mathbf{F}} = \mathbf{F}_p\cdot\mathbf{T}_p,\quad \mathbf{T}_p = \mathbf{V}^{-T}
$$

对非线性问题，采用**Hermite 多项式内插**（利用两端的位移、速度、加速度，即五次多项式）估计中间时刻的状态，从而构造 $[\mathbf{F}_p]$。

### 创新点四：首次系统验证强非线性问题收敛性

以简单摆（强非线性 SDOF）和三自由度非线性弹簧系统为算例，证明：在采用 Hermite 内插时，非线性问题的收敛阶次与线性问题相同（最高可达7阶），在同等精度需求下可使用比二阶方法大若干数量级的时间步长。

---

## 三、论文大致流程与关键公式-代码对应

### 3.1 整体流程概览

```
用户输入: M (近似阶次), ρ∞ (耗散参数), Δt, K, M_mat, C, F, u0, v0
      ↓
初始化方案系数 (InitSchemePadePF / InitSchemeRhoPF)
  ├─ Padé展开 → 获得 rs (根), a (部分分式系数), plr, cfr
  └─ M-格式  → 获得 r (单重根), prcoe (移位系数), cfr
      ↓
确定力采样点 (forceSamplingPoints)  →  Gauss-Lobatto 点 s
      ↓
预分解有效刚度矩阵:  r²M + rC + K  (只做一次)
      ↓
时间循环 (for it = 2:ns):
  ├─ 在 s 处采样外力 Fp = F * signal(ts)
  ├─ z = ρ * z_{n-1}  (高频耗散项)
  ├─ 对每个根 r_i:
  │    g_i = P_L(r_i) * z_{n-1}          (辅助向量)
  │    f_i = Fp * Tcfr(:,i)              (力加权组合)
  │    解线性方程: (r²M+rC+K) x̂_i = ...  (有效刚度方程)
  │    x̄_i = (x̂_i + ḡ_i) / r_i
  │    z  += a_i * x_i                   (叠加子步)
  │    an += a_i * (r_i * x̂_i - ĝ_i)   (向量更新加速度)
  └─ 存储 dsp, vel, acc
```

### 3.2 关键公式与代码逐一对应

---

#### 公式 (10)：状态空间一阶 ODE

$$
\frac{\mathrm{d}\mathbf{z}}{\mathrm{d}s} = \mathbf{A}\mathbf{z}(s) + \bar{\mathbf{f}}(\mathbf{z}(s)),\quad
\mathbf{A} = \begin{bmatrix}-\Delta t\mathbf{M}^{-1}\mathbf{C} & -\Delta t^2\mathbf{M}^{-1}\mathbf{K}\\\mathbf{I} & \mathbf{0}\end{bmatrix}
$$

**代码（TimeSolverPF.m / TimeSolverTRhoPF.m）：**  
$[\mathbf{A}]$ 不显式构造，而是通过对角块操作隐含使用。预处理时对矩阵进行无量纲化缩放：

```matlab
K  = dt*dt*sparse(K);    % → Δt²K
C  = dt*sparse(C);       % → ΔtC
v0 = dt*v0;              % 速度归一化为 Δt·v₀，即无量纲速度
```

初始状态向量为 $[\mathbf{z}_0 = \{\Delta t\dot{\mathbf{u}}_0;\,\mathbf{u}_0\}]$（即 `z0 = [v0; u0]`）。

---

#### 公式 (21)：矩阵指数的有理逼近（Padé 展开）

$$
e^{\mathbf{A}} \approx \mathbf{R} = \frac{P(\mathbf{A})}{Q(\mathbf{A})} = \frac{p_0\mathbf{I}+p_1\mathbf{A}+\cdots+p_M\mathbf{A}^M}{q_0\mathbf{I}+q_1\mathbf{A}+\cdots+q_M\mathbf{A}^M}
$$

**代码（PadeExpansion.m → InitSchemePadePF.m）：**

```matlab
[pcoe, qcoe, rs] = PadeExpansion(M, rhoInfty);
% pcoe: P(x)的系数 [p0,p1,...,pM]
% qcoe: Q(x)的系数 [q0,q1,...,qM]
% rs:   Q(x)的根（不同根情形）
```

对于单重根情形（InitSchemeRhoPF.m），先用 `MschemeRoot` 求解根 $[r]$，再用 `pCoefficients` 得到 $[P(\mathbf{A})]$ 系数。

---

#### 公式 (34)(36)：部分分式系数 $[a_i]$ 与分解

$$
a_i = \frac{1}{\prod_{j\ne i}(r_j - r_i)},\quad
\frac{\mathbf{P}}{\mathbf{Q}} = \frac{p_M}{q_M}\mathbf{I} + \sum_{i=1}^{M}\frac{a_i\,P_L(r_i)}{r_i\mathbf{I}-\mathbf{A}}
$$

**代码（polyPartialFraction.m → InitSchemePadePF.m）：**

```matlab
a   = polyPartialFraction(qcoe, rs);      % 计算 a_i，Eq.(34)
rho = pcoe(end)/qcoe(end);               % ρ = p_M/q_M
plr = plcoe * reshape(rs,1,[]).^((0:M-1)');  % P_L(r_i)，Eq.(37)
```

`polyPartialFraction` 对每个根 $[r_i]$ 按 Eq.(34) 计算乘积倒数：

```matlab
a(ii) = 1/prod(allRoots([1:ii-1,ii+1:end]) - r(ii));
```

---

#### 公式 (43)(81)：外力的加权组合 $[\mathbf{f}_i]$

$$
\mathbf{f}_i = \sum_{k=0}^{p_f}\tilde{\mathbf{f}}^{(k)}C_k(r_i)
= \mathbf{F}_p\,\mathbf{T}_p\,\mathbf{c}\,\mathbf{r}_i
$$

**代码（TimeIntgCoeffForce.m → InitSchemePadePF.m，TimeSolverPF.m）：**

```matlab
% 初始化: 得到系数矩阵 cf（即论文中的 c），然后对各根求值
cf  = TimeIntgCoeffForce(pcoe, qcoe, pf);    % c_ki，Eq.(26)
cfr = cf * reshape(rs,1,[]).^((0:M-1)');     % C_k(r_i)，Eq.(38)
% 与变换矩阵结合得到采样点→力向量的映射
Tcfr = transMtxPointsToPoly(s, p+1) * cfr(1:p+1,:);
```

在时间循环中：

```matlab
Fp  = F .* reshape(signal(ts), 1, []);   % F_p 矩阵（力在采样点处的值）
fri = Fp * real(Tcfr(:,i));              % f_i = F_p * T_p * c * r_i，Eq.(81)
```

`transMtxPointsToPoly` 计算的是 $[\mathbf{T}_p = \mathbf{V}^{-T}]$（Eq.(83)）：

```matlab
a = (s-0.5).^(0:nf-1);   % V^T
T = a/(a'*a);             % T_p = (V^T)^{-1} = V^{-T}
```

---

#### 公式 (73)(74)：有效刚度方程（子步隐式求解）

$$
\left(r_i^2\mathbf{M} + r_i\Delta t\mathbf{C} + \Delta t^2\mathbf{K}\right)\hat{\mathbf{x}}_i
= r_i\mathbf{M}\hat{\mathbf{g}}_i - \Delta t^2\mathbf{K}\bar{\mathbf{g}}_i + r_i\Delta t^2\mathbf{f}_i
$$

**代码（TimeSolverPF.m，实根分支）：**

```matlab
% 预分解（只做一次）:
dKd1 = decomposition(sparse((r*r)*M + r*C + K));   % r²M + rΔtC + Δt²K

% 时间循环中:
g   = real(plr(1)) * z0;                           % g_i = P_L(r_i)*z_{n-1}
fri = Fp * real(Tcfr(:,1));                        % r_i * f_i
% 右端 = r_i*M*ĝ_i - K*ḡ_i + r_i*f_i
ftmp = dKd1 \ (r*(M*g(1:n)) - K*g(n+1:end) + r*fri);  % 解 x̂_i
x    = [ftmp; (ftmp + g(n+1:end))/r];              % x_i = {x̂_i; x̄_i}
```

对复数根使用 LU 分解（效率更高）：

```matlab
[L{ic},U{ic},LUp{ic},LUq{ic}] = lu(sparse((r*r)*M + r*C + K), 'vector');
tmp(LUp{ic},:) = U{ic} \ (L{ic} \ ctmp(LUq{ic},:));
```

---

#### 公式 (45)：时间步进更新（不同根情形）

$$
\mathbf{z}_n = \rho\,\mathbf{z}_{n-1} + \sum_{i=1}^{M} a_i\,\mathbf{x}_i
$$

**代码（TimeSolverPF.m）：**

```matlab
z  = rho * z0;                         % ρ·z_{n-1}
z  = z + real(a(1)) * x;              % 实根贡献
z  = z + 2*real(a(ic+nReal) * x);     % 复数根对贡献（取实部×2）
z0 = z;
```

---

#### 公式 (67)：时间步进更新（单重根情形）

$$
\mathbf{z}_n = p_{rM}\,\mathbf{z}_{n-1} + \mathbf{x}_M
$$

递归求解（Eq. 66）$[\mathbf{A}_r\mathbf{x}_i = \mathbf{b}_{i-1} + \mathbf{x}_{i-1}]$：

**代码（TimeSolverTRhoPF.m）：**

```matlab
x = zeros(2*n, 1);
for ip = 1:p
    g           = x + prcoe(ip)*z;              % g_i = x_{i-1} + p_{ri}·z_{n-1}
    rfri        = Fp * Tcfr1(:,ip);             % r·f_{ri}
    x(1:n)      = dKd \ (r*(M*g(1:n)) - K*g(n+1:end) + rfri);  % x̂_i
    x(n+1:end)  = (x(1:n) + g(n+1:end)) / r;   % x̄_i
end
z = prcoe(p+1)*z + x;    % z_n = p_{rM}·z_{n-1} + x_M，Eq.(67)
```

---

#### 公式 (77)(78)：加速度更新（向量操作，无额外方程求解）

不同根情形：

$$
\overset{\circ}{\mathbf{u}}_n = \rho\,\overset{\circ}{\mathbf{u}}_{n-1}
+ \sum_{i=1}^{M} a_i\!\left(r_i\hat{\mathbf{x}}_i - \overset{\circ}{\bar{\mathbf{g}}}_i\right)
$$

单重根情形：

$$
\overset{\circ}{\mathbf{u}}_n = p_{rM}\,\overset{\circ}{\mathbf{u}}_{n-1}
+ \left(r\hat{\mathbf{x}}_M - \overset{\circ}{\bar{\mathbf{g}}}_M\right)
$$

**代码（TimeSolverPF.m）：**

```matlab
an = rho * a0;                                   % ρ·ü_{n-1}
an = an + real(a(1))  *(r*x(1:n) - g(1:n));     % 实根：a_i*(r*x̂_i - ĝ_i)
an = an + 2*real(a(ic+nReal)*(r*tmp - cg(1:n)));% 复根贡献×2
```

**代码（TimeSolverTRhoPF.m）：**

```matlab
an = prcoe(end)*an + (r*x(1:n) - g(1:n));  % p_{rM}·ü_{n-1} + (r*x̂_M - ĝ_M)
```

最终输出时还原物理量纲（Eq. 6）：

```matlab
vel = vel / dt;       % Δt·u̇ → u̇
acc = acc / (dt*dt);  % Δt²·ü → ü
```

---

## 四、数据流总结（从输入到输出）

| 阶段 | 操作 | 涉及函数/变量 |
|------|------|--------------|
| **初始化** | 根据 $[M]$、$[\rho_\infty]$ 计算 Padé 展开系数、根、部分分式系数 | `InitSchemePadePF` / `InitSchemeRhoPF` |
| **采样点** | 计算 Gauss-Lobatto 点 $[s_k \in [0,1]]$ | `forceSamplingPoints` |
| **变换矩阵** | 计算 $[\mathbf{T}_p = \mathbf{V}^{-T}]$ 及 $[\mathbf{T}_p\cdot\mathbf{c}\cdot\mathbf{r}_i]$ 的合并矩阵 `Tcfr` | `transMtxPointsToPoly` |
| **预分解** | 对每个不同根分解 $[r_i^2\mathbf{M}+r_i\mathbf{C}+\mathbf{K}]$（仅一次） | MATLAB `decomposition`/`lu` |
| **时间循环** | 采样外力 → 按根逐步求解 $[\mathbf{x}_i]$ → 叠加更新 $[\mathbf{z}_n]$ 和 $[\mathbf{a}_n]$ | `TimeSolverPF` / `TimeSolverTRhoPF` |
| **输出** | 还原量纲，存储位移 `dsp`、速度 `vel`、加速度 `acc` | — |

---

## 五、小结

本文的核心贡献在于：**用部分分式展开重构时间步进算法**，将原本需要质量矩阵分解的 $[\mathbf{M}^{-1}\mathbf{f}]$ 操作转化为纯向量组合，同时推导出仅用已有中间变量即可向量更新加速度的公式，彻底释放了高阶自启动格式的计算潜力。新算法在线性与强非线性问题中均达到与理论一致的高阶收敛率，并在波传播问题中显著优于商业软件常用的二阶方法。
